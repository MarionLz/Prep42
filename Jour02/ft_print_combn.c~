#include <unistd.h>

int ft_putchar(char c)
{
  write(1, &c, 1);
  return (0);
}

void ft_print_combn(int n)
{
  if (0 < n < 10)
    {
      int index;
      index = 0;
      while (index <= n)
	{
	  int chiffre;
	  chiffre = 0;
	  while(chiffre <= 9)
	    {
	      
	    }
	}
    }
}

int main()
{

}
